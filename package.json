{
  "name": "sneakerbox",
  "version": "1.0.0",
  "description": "A virtual storefront for athletic shoe lovers",
  "main": "app.js",
  "scripts": {
    "dev": "set NODE_ENV=dev&& nodemon app.js",
    "dev-mac": "NODE_ENV=dev nodemon app.js",
    "prod": "set NODE_ENV=prod && node app.js",
    "prod-mac": "NODE_ENV=prod node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "jFarrow02",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.1.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^1.19.2"
  }
}
